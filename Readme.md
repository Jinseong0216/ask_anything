- master_branch

- develop_branch

CheckList
    - JWT
    - CSRF
    - OAuth
    - Https
    - CORS
    - Rate Limit
    - PWD Security
    - Authority Check(Server Only)

    - Expected Tools
    '''MarkDown
        Frontend (Vue)
        - Composition API
        - Pinia (auth state only)
        - Axios (withCredentials: true)

        Backend (Flask)
        - flask-jwt-extended
        - HttpOnly Cookie JWT
        - CSRF protection
        - flask-limiter
        - bcrypt

        Auth
        - 기본 로그인 + Google OAuth
    '''